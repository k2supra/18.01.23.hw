#include <iostream>
#include <cmath>
using namespace std;

int main() {
	cout << "Enter a number ->";
	int num, step;
	cin >> num;
	cout << "Enter 2nd number ->";
	cin >> step;
	cout << "\n = " << pow(num, step);
}
